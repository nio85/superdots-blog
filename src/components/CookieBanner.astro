<div id="cookie-banner" class="cookie-banner" role="dialog" aria-label="Cookie consent" hidden>
	<div class="cookie-banner-inner">
		<div class="cookie-banner-content">
			<p class="cookie-banner-title">We value your privacy</p>
			<p class="cookie-banner-text">
				We use Google Analytics cookies to understand how you use our site and improve our content.
				No personal data is collected. <a href="/cookie-policy">Learn more</a>
			</p>
		</div>
		<div class="cookie-banner-actions">
			<button id="cookie-reject" class="cookie-btn cookie-btn-secondary" type="button">
				Reject all
			</button>
			<button id="cookie-accept" class="cookie-btn cookie-btn-primary" type="button">
				Accept analytics
			</button>
		</div>
	</div>
</div>

<script is:inline>
(function() {
	var consent = localStorage.getItem('sd-cookie-consent');
	if (consent) return;

	var banner = document.getElementById('cookie-banner');
	if (!banner) return;
	banner.removeAttribute('hidden');
	requestAnimationFrame(function() {
		requestAnimationFrame(function() {
			banner.classList.add('is-visible');
		});
	});

	function hideBanner() {
		banner.classList.remove('is-visible');
		banner.addEventListener('transitionend', function handler() {
			banner.setAttribute('hidden', '');
			banner.removeEventListener('transitionend', handler);
		});
	}

	document.getElementById('cookie-accept').addEventListener('click', function() {
		localStorage.setItem('sd-cookie-consent', 'accepted');
		localStorage.setItem('sd-cookie-consent-date', new Date().toISOString());
		hideBanner();

		// Grant analytics consent — gtag.js is already loaded by BaseHead
		if (window.gtag) {
			window.gtag('consent', 'update', {
				analytics_storage: 'granted'
			});
		}
	});

	document.getElementById('cookie-reject').addEventListener('click', function() {
		localStorage.setItem('sd-cookie-consent', 'rejected');
		localStorage.setItem('sd-cookie-consent-date', new Date().toISOString());
		hideBanner();
	});
})();

/* Allow re-opening cookie settings from footer link */
document.addEventListener('DOMContentLoaded', function() {
	var settingsLink = document.getElementById('cookie-settings-link');
	if (settingsLink) {
		settingsLink.addEventListener('click', function(e) {
			e.preventDefault();
			localStorage.removeItem('sd-cookie-consent');
			localStorage.removeItem('sd-cookie-consent-date');
			var banner = document.getElementById('cookie-banner');
			if (banner) {
				banner.removeAttribute('hidden');
				requestAnimationFrame(function() {
					requestAnimationFrame(function() {
						banner.classList.add('is-visible');
					});
				});
			}
		});
	}
});
</script>

<style>
	.cookie-banner {
		position: fixed;
		bottom: 0;
		left: 0;
		right: 0;
		z-index: 9999;
		background: var(--color-bg);
		border-top: 2px solid var(--color-border-strong);
		padding: var(--space-5) var(--space-4);
		box-shadow: 0 -8px 32px var(--shadow-banner-color, rgba(11, 18, 34, 0.15));
	}
	@supports (backdrop-filter: blur(12px)) or (-webkit-backdrop-filter: blur(12px)) {
		.cookie-banner {
			backdrop-filter: blur(16px) saturate(1.2);
			-webkit-backdrop-filter: blur(16px) saturate(1.2);
			background: rgba(255, 255, 255, 0.88);
		}
	}
	:global([data-theme="dark"]) .cookie-banner {
		--shadow-banner-color: rgba(0, 0, 0, 0.4);
	}
	@supports (backdrop-filter: blur(12px)) or (-webkit-backdrop-filter: blur(12px)) {
		:global([data-theme="dark"]) .cookie-banner {
			background: rgba(11, 18, 34, 0.88);
		}
	}
	@media (prefers-color-scheme: dark) {
		:global([data-theme="auto"]) .cookie-banner {
			--shadow-banner-color: rgba(0, 0, 0, 0.4);
		}
		@supports (backdrop-filter: blur(12px)) or (-webkit-backdrop-filter: blur(12px)) {
			:global([data-theme="auto"]) .cookie-banner {
				background: rgba(11, 18, 34, 0.88);
			}
		}
	}
	.cookie-banner-inner {
		max-width: var(--max-width-content);
		margin: 0 auto;
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: var(--space-6);
		flex-wrap: wrap;
	}
	.cookie-banner-content {
		flex: 1;
		min-width: 280px;
	}
	.cookie-banner-title {
		margin: 0 0 var(--space-1);
		font-family: var(--font-display);
		font-size: var(--text-base);
		font-weight: var(--weight-semibold);
		color: var(--color-text-strong);
	}
	.cookie-banner-text {
		margin: 0;
		font-size: var(--text-sm);
		color: var(--color-text-muted);
		line-height: var(--leading-normal);
	}
	.cookie-banner-text a {
		color: var(--color-primary);
		text-decoration: underline;
		text-underline-offset: 2px;
	}
	.cookie-banner-text a:hover {
		color: var(--color-primary-hover);
	}
	.cookie-banner-actions {
		display: flex;
		gap: var(--space-3);
		flex-shrink: 0;
	}
	.cookie-btn {
		font-family: var(--font-display);
		font-size: var(--text-sm);
		font-weight: var(--weight-semibold);
		padding: var(--space-2) var(--space-5);
		border-radius: var(--radius-md);
		border: 2px solid transparent;
		cursor: pointer;
		transition: all 0.2s ease;
		line-height: 1;
		white-space: nowrap;
	}
	.cookie-btn-primary {
		background: var(--color-primary);
		color: #FFFFFF;
		border-color: var(--color-primary);
	}
	.cookie-btn-primary:hover {
		background: var(--color-primary-hover);
		border-color: var(--color-primary-hover);
		box-shadow: var(--shadow-md);
		transform: translateY(-1px);
	}
	.cookie-btn-secondary {
		background: transparent;
		color: var(--color-text);
		border-color: var(--color-border-strong);
	}
	.cookie-btn-secondary:hover {
		color: var(--color-text-strong);
		border-color: var(--color-text-strong);
	}

	/* Entrance animation */
	.cookie-banner:not([hidden]) {
		opacity: 0;
		transform: translateY(100%);
		transition: opacity 400ms cubic-bezier(0.4, 0, 0.2, 1),
					transform 400ms cubic-bezier(0.16, 1, 0.3, 1);
	}
	.cookie-banner.is-visible {
		opacity: 1;
		transform: translateY(0);
	}
	@media (prefers-reduced-motion: reduce) {
		.cookie-banner:not([hidden]) {
			transform: none;
			transition: opacity 200ms ease;
		}
	}

	/* Focus styles for keyboard users */
	.cookie-btn:focus-visible {
		outline: 2px solid var(--color-primary);
		outline-offset: 2px;
	}
	.cookie-btn-secondary:focus-visible {
		outline-color: var(--color-text-strong);
	}

	@media (max-width: 600px) {
		.cookie-banner-inner {
			flex-direction: column;
			align-items: stretch;
			gap: var(--space-4);
		}
		.cookie-banner-actions {
			justify-content: stretch;
		}
		.cookie-btn {
			flex: 1;
			text-align: center;
			padding: var(--space-3) var(--space-4);
		}
	}
</style>
